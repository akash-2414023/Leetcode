For this type of question as dont know the search space so for searching that we will initialise 
low = 0 ;
high = 1;

//Full code

int binarySearch(vector<int>& nums,int low,int high,int target){
  while(low <= high){
    int mid = low + (high - low) / 2;
    if(nums[mid] == target) return mid;
    else if(nums[mid] < target) low = mid + 1;
    else high = mid - 1;
  }
  return -1
}
int binarySearchinInfiniteArray(vector<int>& nums,int target){
  int n = nums.size();
  int low = 0,high = 1;
  //expand the range till target <= nums[high]
  while(nums[high]<target){
    low = high + 1;
    high = high*2;
  }
  return binarySearch(nums,low,high,target)
}
  
    
  

  
