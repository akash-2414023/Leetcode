long long binExpo(long long base, int expo) {
        if (expo == 0)
            return 1;

        long long half = binExpo(base, expo / 2);

        if (expo % 2 == 0)
            return half * half;

        return half * half * base;
}
